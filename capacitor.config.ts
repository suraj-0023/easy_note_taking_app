import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.surajkunuku.nexora',
  appName: 'Nexora',
  webDir: '.',
  server: {
    androidScheme: 'https'
  }
};

export default config;
